{
  "name": "address-verification-app",
  "version": "1.0.0",
  "description": "Address verification service using Gemini and Supabase.",
  "main": "api/verify-single-address.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.44.4"
  }
}
